language: java

sudo: required

services:
  - docker

cache:
  directories:
    - '$HOME/.m2/repository'

script:
- mvn verify
- mvn coveralls:report

deploy:
  provider: script
  script: mvn deploy:deploy --settings=./.travis/settings.xml
  on:
    branch: master